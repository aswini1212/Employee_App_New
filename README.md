----RUFF----
ruff check . --fix #runs linter on all files and automatically fixes issues like unused import
ruff format . #formats code according to ruff's formatting style



----dokcer build-------
docker compose up --build --watch
